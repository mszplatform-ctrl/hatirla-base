import React from 'react';
import Referral from './components/Referral';

function PackagePage() {
    return (
        <div>
            <h2>AI Paket Önerileri</h2>
            {/* Referral component burada */}
            <Referral />
        </div>
    );
}

export default PackagePage;
